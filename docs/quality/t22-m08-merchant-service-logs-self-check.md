# T22 M08 商家端服务状态与日志入口自检

日期：2026-06-16

结论：通过。第 8 项只完成商家端服务状态、请求日志和绘图日志入口，不开始第 9 项。

## 完成内容

- 新增商家端服务状态页，展示真实平台组件、数据库、前端探针、外部监控和上游状态。
- 新增商家端请求日志页，读取真实请求记录，并只展示安全字段。
- 新增商家端绘图日志页，只读取真实 `kind=image` 记录，通用异步任务不进入该页。
- 商家端固定栏中保留请求日志、绘图日志、服务状态入口，不加入没有实际上游支撑的“异步任务”独立入口。

## 真实数据检查

- `npm run qa:t22:merchant-service-logs` 通过。
- 检查脚本临时创建后台账号、普通用户、令牌、请求日志、一条图片任务和一条通用任务。
- 已验证商家账号能读取真实请求日志。
- 已验证普通用户不能访问商家日志能力。
- 已验证绘图日志只显示图片任务，不显示通用异步任务。
- 已验证用户端日志页和服务状态页仍能打开。
- 清理结果：临时用户、钱包、会话、令牌、请求日志和任务残留均为 0。

## 页面检查

- `npm run typecheck` 通过。
- `npm run build` 通过。
- 本地 Chrome 真实登录 `merchant_test_1 / merchant200611` 通过。
- 已验证页面：`/merchant/service-status`、`/merchant/request-logs`、`/merchant/drawing-logs`。
- 已验证宽度：1366 和 390。
- 页面结果：固定顶部栏和左侧栏存在，无“异步任务”误导入口，无 500，无旧英文品牌，无横向溢出。
- 截图证据：`.gstack/qa-reports/screenshots/t22-m08-*.png`。

## Review 结论

- 本次新增商家日志能力仍由后台权限保护，普通用户访问被拒绝。
- 请求日志响应不返回令牌哈希、上游密钥、连接串或充值码哈希。
- 绘图日志响应按 `kind=image` 过滤，避免把尚未接入的通用异步任务包装成可用能力。
- 空数据时显示真实空状态，没有使用假数据填充列表。

## 副手复核

- 已尝试调用 Codex GPT-5.3-Codex-Spark 副手做只读复核。
- 副手未完成，原因是本机副手连接被拒绝并超时。
- 本轮最终计划、实现、检查和审查结论由主 Codex 本地完成。

## 剩余边界

- 当前 MVP 只做平台老板统一管理，不做多商家独立上游密钥隔离。
- 当前未接入真实绘图提交入口，绘图日志只展示已经写入的真实图片任务记录。
- 第 9 项仍未开始，后续需要做全量角色隔离和端到端回归。
