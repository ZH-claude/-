# T23 M01 上线前验收基线自检

日期：2026-06-16

结论：通过。第 1 项已完成上线前验收基线和阻塞清单；本轮没有冒充真实云服务器部署，也没有用假数据替代真实检查。

## 任务选择

- T22 商家端独立化已完成。
- T21 真实云服务器部署仍缺服务器 SSH、域名 DNS、生产 `.env` 和真实 strict smoke 账号。
- 因 T21 外部条件缺失，本轮进入 T23-M01，先建立上线前验收基线和阻塞清单。
- T21 不标记完成，T23 也不整体标记完成；本轮只完成 T23-M01。

## 完成内容

- 新增 `docs/product/prelaunch-acceptance-plan.md`。
- 拆分 T23 M01-M07：验收基线、生产 strict smoke、账单核对、压测限流、安全权限、运维演练、上线决策报告。
- 明确真实数据规则：账号、会话、上游、模型、余额、计费、日志和测试数据都必须来自真实接口或真实数据库。
- 明确功能接口预留边界：当前 MVP 是单平台老板模式；多商家独立上游 Key、客户按商家归属转发、商家独立账单和商家独立风控均未实现，不能伪造成已完成。
- 明确当前阻塞项：服务器 SSH、域名 DNS、生产 `.env`、真实上游 Key、真实 smoke 账号和真实通知渠道。

## 本轮真实检查结果

本轮完成文档基线后，已复跑以下真实检查：

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 通过 |
| `npm run build` | 通过 |
| `npm run qa:t22:merchant-role-isolation` | 通过；真实创建普通用户和商家账号，真实登录，验证用户端和商家端权限，清理后残留为 0 |
| `npm run qa:t20:observability` | 首次用本机 3104 服务失败，原因是可计费请求访问临时上游返回 500；随后按当前 Docker 服务入口 3001 复跑通过 |
| `npm run smoke:t21:deploy` | 通过基础项；登录、令牌、模型、trace、chat、充值和通知因缺少真实生产配置显示 `skip` |
| Chrome 首页截图 | 通过；`t23-m01-home.png` 确认首页可被真实浏览器加载，不是空白或错误页 |

`npm run qa:t20:observability` 复跑通过时，真实检查了模型列表日志、前置拒绝日志、可计费请求、上游 500、malformed 响应、跨用户 trace 隔离、真实数据库请求日志和敏感字段白名单；清理后临时用户、会话、钱包、令牌、用量事件、钱包流水、请求日志、上游、模型和价格残留均为 0。

## QA 口径

- `pass` 表示真实检查通过。
- `skip` 表示真实条件缺失，不等于通过。
- `fail` 表示必须修复或明确阻塞。
- 临时用户、钱包、会话、请求日志、用量事件、钱包流水等检查数据必须清理为 0 残留。

## Review 重点

- 双端兼容：普通用户只能进入用户端，商家账号才能进入商家端。
- 数据一致：成功调用必须能串起请求日志、用量事件、钱包流水和余额变化。
- 接口预留：未来多商家隔离只写边界和验收，不伪造角色或假表。
- 部署真实：无服务器和域名时，不得把 T21 写成生产完成。
- 密钥安全：文档、脚本和仓库都不能包含真实密码、真实上游 Key 或生产 `.env`。

## Review 结论

- 双端兼容通过：T22 真实互通脚本确认普通用户和商家账号分流正确，权限不串门。
- 数据一致通过：T20 真实追踪脚本确认请求日志、用量事件、钱包流水和上游状态能按同一请求编号追踪。
- 接口预留合格：T23 计划只标注未来多商家能力边界，没有新增假角色、假商家表或假上游。
- 部署真实合格：T21 仍保持未完成；smoke 中缺真实生产条件的项目保留 `skip`。
- 密钥安全通过：扫描只发现 `.env.example` 占位值、文档示例和 QA 预览值，没有发现真实密码、真实上游 Key 或生产 `.env`。

## 副手复核

- 已尝试调用 Codex GPT-5.3-Codex-Spark 副手做只读复核。
- 副手未完成，原因是模型刷新超时、连接被拒绝和流断开。
- 本轮任务选择、文档、验证和审查由主 Codex 完成。

## 当前剩余风险

- 真实云服务器部署还未执行，生产访问能力未知。
- 真实上游 Key 未配置，不能完成真实 chat strict smoke。
- 真实通知渠道未配置，通知 strict smoke 只能保持阻塞。
- 压测和账单核对还未进入执行阶段，本轮只是建立验收基线。
