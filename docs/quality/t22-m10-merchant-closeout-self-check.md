# T22 M10 收尾、文档与部署检查自检

日期：2026-06-16

结论：通过。第 10 项已完成收尾说明、部署边界、测试账号安全说明和真实互通检查；T22 商家端独立化闭环完成。T21 真实云服务器部署仍未执行，不能标记为生产上线完成。

## 完成内容

- README 已补充用户端和商家端关系：同一套 Web、同一个登录入口，普通用户进入用户端，商家/后台账号进入商家端。
- 云服务器部署手册已补充商家端部署边界：生产不需要第二个商家端前端。
- `.env.example` 已增加空的 `MERCHANT_TEST_PASSWORD`，只作为本地/测试环境创建商家测试账号的入口，不包含真实密码。
- 商家端计划表已将 M10 标记为完成，并写明当前 MVP 与未来多商家隔离的边界。
- 总开发计划已将 T22 标记为完成；T21 保持未完成，因为真实服务器、域名、证书和公网 strict 检查尚未执行。
- 仓库文档中的商家端测试密码明文已改为 `MERCHANT_TEST_PASSWORD`，保留账号名 `merchant_test_1`、`merchant_test_2`、`merchant_test_3`。

## 真实互通检查

- `npm run qa:t22:merchant-role-isolation` 通过。
- 检查使用真实数据库、真实临时普通用户、真实临时商家账号、真实登录会话和真实钱包。
- 已验证商家账号登录后进入商家端，普通用户登录后进入账户中心。
- 已验证普通用户无法访问全部商家端页面和后台能力。
- 已验证商家账号可访问商家端核心页面和后台能力。
- 已验证用户端核心页面仍可访问：账户、充值、令牌、日志、价格、分组状态、通知、绘图日志和服务状态。
- 已验证商家端核心页面仍可访问：商家首页、旧后台兼容页、用户、充值码、上游/模型、公告、审计、服务状态、请求日志和绘图日志。
- 清理结果：临时用户、钱包、会话和安全审计残留均为 0。

## 部署检查

- `npm run smoke:t21:deploy` 在本地等价环境通过。
- 已通过真实 HTTP 检查 API 健康、Web 首页和服务状态。
- 因本轮没有配置真实生产账号、真实 API Key、真实模型、真实充值码和真实通知渠道，登录、令牌、模型、trace、chat、充值和通知项按脚本规则显示 `skip`，没有伪造成通过。

## 工程检查

- 首次把 `npm run typecheck` 和 `npm run build` 并行执行时失败，原因是两个检查同时刷新同一份本地生成文件，互相抢占。
- 已按顺序重跑：
  - `npm run typecheck` 通过。
  - `npm run build` 通过。
- `git diff --check` 未发现空白格式错误，仅提示 Git 会按仓库规则处理换行。

## 安全检查

- 未提交 `.env`。
- 未提交真实上游 API Key。
- 未提交真实数据库密码。
- 商家测试账号脚本仍要求 `MERCHANT_TEST_PASSWORD`，脚本中没有硬编码测试密码。
- 密钥扫描只发现占位值、文档示例、token 前缀常量和 QA 预览值，没有真实密钥。

## Review 结论

- 业务边界清楚：当前是单平台老板模式，你在商家端录入另一个中转站的上游地址和 API Key，客户使用本平台发放的 Key 调用。
- 未伪造商用多商家能力：多商家各自独立上游 Key、客户按商家归属转发，仍在商用升级计划中。
- 部署边界清楚：云服务器只需要一套 Web；商家端和用户端由账号身份分流。
- 数据真实性符合要求：本轮检查使用真实数据库和真实登录会话，没有用假数据冒充通过。

## 副手复核

- 已尝试调用 Codex GPT-5.3-Codex-Spark 副手做只读复核。
- 副手未完成，原因是模型刷新超时、连接被拒绝和流断开。
- 本轮最终计划、文档、验证和审查结论由主 Codex 完成。

## 剩余边界

- T21 真实云服务器部署未完成：仍需要服务器 SSH、域名 DNS、生产 `.env`、公网 HTTPS 和真实 strict smoke 账号。
- T23 上线前验收未完成：仍需要压测、安全检查、账单核对和运维手册最终核对。
- 商用多商家隔离未完成：需要新增商家归属、客户归属、商家独立上游密钥和按商家转发的真实验收。
