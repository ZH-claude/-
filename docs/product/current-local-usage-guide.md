# 当前本地使用说明

更新时间：2026-06-19

## 当前计费方向

当前本地版本正在向统一 token 额度收口：商家按人民币生成充值码，用户兑换后得到 token；token 只记录上游真实返回的输入和输出用量，价格按模型的输入价、输出价和价格倍率计算。

新的完整规则和任务拆分见：`docs/product/token-metering-upgrade-plan.md`。

## 先看这两个数字

商家端仍保留人民币面值，方便你按人民币生成充值码和看运营金额；用户端显示真实 token 用量和美元消费，客户调用时扣的是按价格规则算出来的金额，不把 token 数量乘大。

| 页面文字 | 它代表什么 | 它不代表什么 |
| --- | --- | --- |
| 客户剩余 token | 所有客户钱包里还没用完的 token 总和 | 不是你的银行卡余额，不是平台利润 |
| 今日客户扣费 | 今天客户调用模型后被扣掉的额度 | 不是上游成本，不是最终利润 |
| 累计客户扣费 | 客户历史调用后累计被扣掉的额度 | 不是已提现收入 |

利润要等后续做“上游成本”和“客户扣费”的对账表后才能准确看。现在这个首页先帮你看客户额度池和客户当天使用情况。

## 商家端怎么用

1. 进入商家首页，先看客户剩余 token、今日客户扣费、活跃用户、上游状态。
2. 打开“模型发布”，先准备客户看到的模型名，例如 `gpt5.5`，这里只管模型名、展示名和启停状态。
3. 打开“DeepSeek 上游”或“中转站上游”，分别接入供应商线路，填写你从供应商拿到的上游地址和密钥，保存后做健康检查。
   同一个商家会同时有两类上游，但不允许混在一个入口里操作。
4. 打开“模型映射”，给这个客户模型绑定到唯一一条上游线路。
   需要低消耗和中消耗时，要发布成两个客户模型，分别绑定不同上游。
5. 如果模型填错了，在“已发布客户模型”里点“修改”；没有绑定线路时可以改模型名，已经绑定线路后一般只能改展示名和启停状态。
6. 打开“用户管理”，查看客户、客户额度、累计扣费和账号状态。
7. 打开“充值码”，填写人民币面值并生成充值码，客户兑换后会得到 token。
8. 打开“请求日志”，查看客户真实调用记录。

## 用户端怎么用

1. 普通用户登录后进入个人中心，看自己的余额、累计消耗、调用次数和可用模型，单位统一显示为 token。
2. 需要加额度时，进入余额充值，输入商家发放的充值码。
3. 需要调用接口时，进入“令牌”页面管理自己的调用密钥。
4. 令牌页面上方可以按令牌名称、可用模型、状态查询；点“重置”会清空筛选条件。
5. 点“新增”创建新令牌，只需要填写名称、可选额度、过期时间、可用模型和备注；不想限制额度或过期时间就留空。
6. 表格每一行右侧可以复制、删除、重置密钥、修改。修改只改令牌信息；重置密钥会生成新的完整密钥。
7. 完整密钥只会在新增或重置后显示一次，必须当场复制保存；之后页面只能复制令牌名称和部分预览。
8. 点“多选”后可以勾选多条令牌一起删除；点“导出”会导出当前筛选结果。
9. 调用后，用户可以在日志页面查看真实调用记录。

## 充值和扣费怎么走

1. 商家先生成充值码，商家端按人民币显示和填写。
2. 用户兑换充值码后，用户端按 token 显示余额。
3. 用户调用模型时，系统按“模型映射”里对应唯一线路扣费，token 按上游真实返回显示，金额按该模型的输入价、输出价和价格倍率计算。
4. 调用成功才扣费；调用失败不会扣客户余额。
5. DeepSeek 普通模型按 1 倍价格计费；其他模型按商家在该线路设置的价格倍率计费，但 token 数量不放大。
6. 中转站上游按商家输入价格加 10% 后计算扣费金额，token 数量仍按上游返回值展示。
7. 现在默认归属倍率保持为 1，不需要商家或用户手动操作。

## 外部工具怎么接

同一把用户端令牌可以用于多种工具。先在用户端“令牌”页面新增或重置令牌，并立刻复制完整密钥。

现在你的正式域名还没有接入本项目，所以先用本地地址测试。上线后把 `127.0.0.1:3001` 换成你服务器上的正式接口域名即可，推荐先预留成 `https://api.你的域名.com`。

| 工具类型 | 本地地址 | 上线后地址 |
| --- | --- | --- |
| OpenAI 兼容工具 | `http://127.0.0.1:3001/v1` | `https://api.你的域名.com/v1` |
| Claude Code / CCSwitch | `http://127.0.0.1:3001` | `https://api.你的域名.com` |
| Codex / Responses 兼容工具 | `http://127.0.0.1:3001/v1` | `https://api.你的域名.com/v1` |
| OpenCode | 选择 OpenAI 方式填 `/v1` 地址；选择 Claude 方式填不带 `/v1` 的地址 | 同左，换成上线域名 |

当前已经兼容这些入口：

| 入口 | 用途 | 状态 |
| --- | --- | --- |
| `/v1/models` | 模型列表 | 已接入 |
| `/v1/chat/completions` | OpenAI 兼容聊天 | 已接入 |
| `/v1/responses` | Codex / 新版 OpenAI Responses | 已接入 |
| `/v1/messages` | Claude Code / CCSwitch | 已接入 |
| `/v1/messages/count_tokens` | Claude Code 计数检查 | 已接入，本地估算，不调用上游 |

### Claude Code / CCSwitch 本地示例

```powershell
$env:ANTHROPIC_API_KEY="用户端复制的完整令牌"
$env:ANTHROPIC_AUTH_TOKEN="用户端复制的完整令牌"
$env:ANTHROPIC_BASE_URL="http://127.0.0.1:3001"
$env:ANTHROPIC_MODEL="你发布给客户看的模型名"
$env:ANTHROPIC_DEFAULT_SONNET_MODEL="你发布给客户看的模型名"
$env:ANTHROPIC_DEFAULT_OPUS_MODEL="你发布给客户看的模型名"

claude --model "你发布给客户看的模型名"
```

Claude Code 建议把同一把用户端完整令牌同时填到 `ANTHROPIC_API_KEY` 和 `ANTHROPIC_AUTH_TOKEN`。前者走常规密钥字段，后者走授权字段；我们的接口两种都接受。改完变量后必须重新打开一个新的 PowerShell，再启动 Claude Code。

### Codex / OpenAI 兼容本地示例

```powershell
$env:OPENAI_API_KEY="用户端复制的完整令牌"
$env:OPENAI_BASE_URL="http://127.0.0.1:3001/v1"
```

### OpenCode 怎么选

OpenCode 如果选择 OpenAI 兼容方式，就填 `http://127.0.0.1:3001/v1`；如果选择 Claude 兼容方式，就填 `http://127.0.0.1:3001`。两种方式用的都是用户端生成的完整令牌。

上线后只把地址换掉，令牌仍然使用用户端生成的完整令牌。

## 关于“分组”

页面上已经不再让商家或用户操作“分组”。系统内部仍保留默认归属，用来保证已有客户能继续看到可用模型，不影响充值、令牌、价格和调用。

旧的分组页面入口现在会自动回到正确位置：普通用户回个人中心，商家账号回商家首页。

## 2026-06-19 商家端正确操作顺序

现在商家端要按这个顺序用：

1. 打开 `模型发布`。
   先发布客户看到的模型，例如 `gpt5.5`。这个模型名就是用户端看到、客户调用时填写的名字。

2. 打开 `DeepSeek 上游` 或 `中转站上游`。
   这里只填上游供应商的信息：上游名称、上游地址、上游密钥、启用状态。保存后可以点检查，确认这个上游能不能连通。

3. 打开 `模型映射` 页面，给这个客户模型绑定唯一上游线路。
   选择刚才保存好的上游，再填写真实上游模型名、超时时间、附加提示词和是否支持流式输出。保存后，这条线路才会参与真实转发。

4. 如果要准备多个成本档位，就发布多个客户模型。
   例如后台可以让 `gpt5.5 低消耗` 走 DeepSeek，让 `gpt5.5 中消耗` 走中转站 1。不要让同一个客户模型背后挂两条上游。

5. 用户端只看客户模型。
   用户选择哪个客户模型，就固定走这个模型背后的商家配置；用户看不到 DeepSeek、中转站、上游地址、上游密钥或后台线路。

不要在上游页面发布客户模型。上游页面只管供应商接入，`模型发布` 只管客户看到的模型名，`模型映射` 才管这个模型实际走哪条上游线路。

当前商家端导航也按这个顺序排列：`模型发布` 在 `DeepSeek 上游` 和 `中转站上游` 前面，最后才是 `模型映射`。如果浏览器里还看到旧顺序，先刷新页面；如果仍然不变，说明本地前端服务还在跑旧进程，需要重启前端。

## 2026-06-19 纠正后的商家端理解

你可以把商家端理解成三个独立仓库：

1. `模型发布`：先把要卖给客户的模型名准备好，例如 `gpt5.5`。
2. `DeepSeek 上游` / `中转站上游`：分别维护供应商地址和密钥，两个上游不能混在一起。
3. `模型映射`：把客户模型绑定到唯一上游线路，并在这里决定这条线路怎么扣费。

当前已经按这个方向收口：一个客户模型只启用一条线路。如果需要低消耗和中消耗，就发布两个客户模型，并分别绑定各自上游。用户端只看到你包装后的模型名、token 用量和扣费金额，不显示 DeepSeek、中转站、上游地址或后台线路。

如果某个已发布模型调用报错，先不要去上游页重新“发布模型”。正确排查顺序是：

1. 在 `模型发布` 确认客户模型还存在、状态是启用。
2. 在 `DeepSeek 上游` 或 `中转站上游` 确认供应商地址和密钥保存正确、健康检查正常。
3. 在 `模型映射` 确认客户模型已经绑定至少一条启用线路，且真实上游模型名、超时时间、提示词、扣费规则保存正确。
4. 再用用户端令牌调用客户模型。

也就是说，模型名、供应商、线路是三件事；不能再把上游接入当成模型发布。
